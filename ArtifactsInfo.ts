import type { ArtifactInfo } from "./ArtifactInfo";
/**
 * Represenation of multiple datasets
 */
export type ArtifactsInfo = {
  /**
   * List of artifacts
   */
  artifacts: Array<ArtifactInfo>;
};
